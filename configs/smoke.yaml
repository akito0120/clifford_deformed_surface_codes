experiment:
  name: smoke
  seed: 0

codes:
  - id: css
    builder: rotated_surface
  - id: xzzx
    builder: xzzx

sampling:
  decoder: mwpm
  max_shots: 300000
  max_errors: 30000
  max_batch_size: 1024
  workers: 4

output:
  dir: results/smoke
  figures: false
  diagrams: false
